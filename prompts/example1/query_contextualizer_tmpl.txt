Produce a JSON list of sub-queries from the user's last message.
Each sub-query: `query` (rewritten search string) and `temporal_filters` (list or null).

Current date: {current_date}
Language for `query` field: {query_language}
Timestamps: UTC (`+00:00`). Week starts Monday.

# Rules
- Rewrite the last `user:` line as one standalone sentence. Use earlier turns only to resolve pronouns. Do not answer.
- Greetings / thanks: copy verbatim, `temporal_filters: null`.
- Emit `temporal_filters` when the user refers to WHEN a document was created / published / written. Leave null if the date only describes the topic.
- Emit MULTIPLE sub-queries only for exclusions ("X except Y") or clearly independent dimensions. Otherwise ONE.

# Temporal filters
Each predicate: `field="created_at"`, `operator` ∈ {{`>=`, `>`, `<=`, `<`}}, `value` = ISO 8601 UTC.
Predicates are AND-combined. Always use half-open intervals: `>= start` AND `< next_start`.

Date resolution (vs Current date):
- today / now → `[today, tomorrow)`
- yesterday → `[yesterday, today)`
- this / last week → Monday-to-Monday
- this / last month → 1st-to-1st
- this / last year → Jan 1-to-Jan 1
- past N days/weeks/months/years → `[Current − N, tomorrow)`
- recent / latest → treat as "past 30 days"
- since X → one predicate `>= X`
- before X → one predicate `< X`
- bare MONTH (no year) → nearest past occurrence
- in YEAR → `[Jan 1 YEAR, Jan 1 YEAR+1)`

For exclusions, split into two sub-queries covering each remaining range. Never negate with a single predicate.

# Examples (Current date = Wednesday, April 15, 2026)

User: "Summary of meetings in the past month"
{{"query_list":[{{"query":"Summary of meetings held in the past month","temporal_filters":[{{"field":"created_at","operator":">=","value":"2026-03-15T00:00:00+00:00"}},{{"field":"created_at","operator":"<","value":"2026-04-16T00:00:00+00:00"}}]}}]}}

User: "Documents from last year except March"
{{"query_list":[{{"query":"Documents from January or February 2025","temporal_filters":[{{"field":"created_at","operator":">=","value":"2025-01-01T00:00:00+00:00"}},{{"field":"created_at","operator":"<","value":"2025-03-01T00:00:00+00:00"}}]}},{{"query":"Documents from April to December 2025","temporal_filters":[{{"field":"created_at","operator":">=","value":"2025-04-01T00:00:00+00:00"}},{{"field":"created_at","operator":"<","value":"2026-01-01T00:00:00+00:00"}}]}}]}}

User: "Q3 2024 reporting template"
{{"query_list":[{{"query":"Q3 2024 reporting template","temporal_filters":null}}]}}
